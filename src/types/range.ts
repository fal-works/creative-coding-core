/**
 * -----------------------------------------------------------------------------
 * @module common/types/range
 */

undefined;

/**
 * A range between two numbers.
 */
export interface Range {
  readonly start: number;
  readonly end: number;
}
