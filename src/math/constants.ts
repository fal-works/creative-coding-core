export const { E, LN10, LN2, LOG2E, LOG10E } = Math;

export const ONE_HALF = 1 / 2;
export const ONE_THIRD = 1 / 3;
export const TWO_THIRDS = 2 / 3;
export const ONE_QUARTER = 1 / 4;
export const TWO_QUARTERS = ONE_HALF;
export const THREE_QUARTERS = 3 / 4;

/**
 * √2
 */
export const SQUARE_ROOT_TWO = Math.SQRT2;

/**
 * √(1 / 2) = 1 / √2 = √2 / 2
 */
export const SQUARE_ROOT_ONE_HALF = Math.SQRT1_2;

/**
 * √3
 */
export const SQUARE_ROOT_THREE = Math.sqrt(3);

/**
 * 1 / √2 = √(1 / 2) = √2 / 2
 */
export const ONE_OVER_SQUARE_ROOT_TWO = SQUARE_ROOT_ONE_HALF;

/**
 * 2 / √2 = √2
 */
export const TWO_OVER_SQUARE_ROOT_TWO = SQUARE_ROOT_TWO;

/**
 * 1 / √3
 */
export const ONE_OVER_SQUARE_ROOT_THREE = 1 / SQUARE_ROOT_THREE;

/**
 * 2 / √3
 */
export const TWO_OVER_SQUARE_ROOT_THREE = 2 / SQUARE_ROOT_THREE;

/**
 * √3 / 2
 */
export const SQUARE_ROOT_THREE_OVER_TWO = SQUARE_ROOT_THREE / 2;

/**
 * √2 / 2 = √(1 / 2) = 1 / √2
 */
export const SQUARE_ROOT_TWO_OVER_TWO = SQUARE_ROOT_ONE_HALF;
